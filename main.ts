// Theme contributions are declared in manifest.toon.
// This no-op entry keeps the pack installable on Katari builds that still
// require manifest.main while the extension itself executes no code.
export {};
